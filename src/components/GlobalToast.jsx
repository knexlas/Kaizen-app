/** kaizen:toast is handled in App and shown via RewardOverlay only (single feedback path). This component no longer subscribes to kaizen:toast to avoid duplicate feedback. */
export default function GlobalToast() {
  return null;
}
